COFFEE MACHINE
OPERATION MODE
Introduction
These days, software supports a large number of machines. With the software, the machines'
mistake rates have lowered while their speed has increased. The infrastructure of many of the
machines we use at home or on the job has code in it. Depending on the needs and usage
locations, these programming languages may change. Coffee culture has blended with
technology as it has developed, becoming a machine. With the help of technology, purchasing
coffee has become more accessible and is now possible in practically any location.
Coffee machines have evolved into devices that come in a variety of sizes and may be found in
many locations. Most businesses manufacture their own coffee makers and provide their own
software for support. In addition to making life easier for people, this also makes it simpler to
meet demand.
In this presentation, we'll examine at a software program that's inside the coffee maker and
describe how it works.
The main menu of the application allows you to select whether you want your coffee hot or
cold. After you choose one of these, program leads you to hot or cold menu.
The codes that enable the application to serve the user hot or cold coffee are shown here.
The user will have chosen the "hot" option if they press "1"
The user will have chosen the "cold" option if they press "2"
The user will have chosen the "Exiting From The Machine" option if he or she presses "3"
All of this is made possible by some computer programming.
because "sleep" is a "return type," for instance. If the function is properly completed, the value
returned will be 0, else it will be less than the value of the time period supplied. The return type
of the sleep function is an integer.
The code uses a lot of equal signs and exclamation marks. There are various ways to use these
signs. If both operands have the same value, the equal-to operator (==) returns true; otherwise,
it returns false. If the operands do not have the same value, the not-equal-to operator (!=)
returns true; otherwise, it returns false.
You can view my coffee maker's home page in this section. You would initially view this
page and the text if you came across our coffee-buying device. You can see the sizes of
the coffees the machine can make on this screen, and you may key in the size and type
of coffee you want to the machine in the appropriate sequence.
The "cout" command: The cout object in C++ is an object of class iostream. It is defined
in iostream header file. It is used to display the output to the standard output device i.e.
monitor. It is associated with the standard C output stream stdout.
There are 50 pieces for each beverage inside the machine.
Int: int - stores integers (whole numbers), without decimals, such as 123 or -123. double
- stores floating point numbers, with decimals, such as 19.99 or -19.99. char - stores
single characters, such as 'a' or 'B'.
Additionally, you can examine the programs needed for money calculations and
currency conversions in this section.
Char: Char is a C++ data type designed for the storage of letters. Char is an abbreviation
for an alphanumeric character. It is an integral data type, meaning the value is stored as
an integer. A char takes a memory size of 1 byte.
Double: The C++ double is also a primitive data type that is used to store floating-point
values up to 15 digits. In the above example, variables of float and double types are
initialized with floating-point values.
Bool: This keyword is a built-in type. A variable of this type can have values true and
false . Conditional expressions have the type bool and so have values of type bool
.(True=1 , False=0)
We established our coffee number 1 and the size and price details of our coffee in this part,
which we created as an empty section called "case 1." Negative "coldBrew—" signs subtract the
amount of coffee entered from the machine's quota.
The "while" loop is intended to execute many programs simultaneously. The user is prompted
to enter the coffee size in this section. If the user inputs a value other than the coffee size, the
computer executes the command "break" and issues a "cout" error message to the user. The
program advances to the next stage if the user specifies the appropriate coffee size.
In this part, we define the costs for the various coffee sizes in the event that the 'if' loop is used
to determine the appropriate coffee size. The program will skip the 'else-if' commands and go
to the next phase if the user selects the "S" size. However, if the user selects a "M" or "L" size,
the software will check the "else-if" instruction.
If the coffee size selection is incorrect in this part, the "false" command stops the while loop.
The program will move on to the following stage if the user enters the proper size.
The program asks the user to enter a coin if the amount of coffee they choose in this part is
greater than 0. The 'if' loop will again display an error and ask the user to enter a valid coin type
if the type of coin entered is of a different type.
The quantity of hot chocolate will decrease as clients choose it.
It won't operate if there is no hot chocolate in excess of 0.
Changing a control variable will end the loop.
Input from the user will increase markCount and add 1 mark to the total amount of
money.
The machine will compute change, lower markCount, and add money to the total
amount of money if the user inserts more money than is necessary.
Cin: Thus, cin means "character input". The C++ cin object belongs to the istream class.
It accepts input from a standard input device, such as a keyboard, and is linked to stdin,
the regular C input source. For reading inputs, the extraction operator(>>) is combined
with the object cin.
if Statement: The if statement evaluates the condition inside the parentheses ( ) . If the
condition evaluates to true , the code inside the body of if is executed. If the condition
evaluates to false , the code inside the body of if is skipped.
Break: The break statement ends execution of the nearest enclosing loop or
conditional statement in which it appears. Control passes to the statement that follows
the end of the statement, if any.
Else: Use else to specify a block of code to be executed, if the same condition is false.
Use else if to specify a new condition to test, if the first condition is false. Use switch to
specify many alternative blocks of code to be executed.
The software asks the user to choose the coffee size at this point. They come in S, M,
and L. The user will be prompted to choose one of the size options if he types a different
character, causing the software to crash.
The cost of a coffee varies depending on the size and type of coffee chosen by the
consumer, and prices might go up or down.
The software issues a "insufficient balance" warning and requests additional funds if the
user deposits missing money. The program will stop working and you won't get your
coffee if you don't pay the remaining balance.
The program will ask you when the coffee is done and if you want another coffee if the
type of coffee you choose is not available.
The machine brews the coffee and serves it to you if the coffee you choose is available.
Function Of Moneyprocces
In that function, we created a do-while loop.
In The First, If statement: If the taken input from the user is less than the price of the coffee we
called the addBalance function.
In the Second If statement: if the taken input from the user is equal to the price of the coffee we
used the calcualte2 variable with the pointer and we equal the calculate2 to 0 and then we used
it to continue the preparation of the coffee.
In the Third and last if statement: if the taken input from the user is more than the price of the
coffee we used calculate2 with the pointer and create a simple mathematic in order the give the
change to user.
